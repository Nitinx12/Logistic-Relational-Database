# LRDB — Logistic Relational Database

Production-style data platform with bronze→silver→gold lakehouse, Postgres serving, and an AI agent that acts only through a validated Java API.

See `ARCHITECTURE.md` for system design and `AGENTS.md` for contribution rules. Run `make up` to start the core stack, `make check` to run CI checks locally.
