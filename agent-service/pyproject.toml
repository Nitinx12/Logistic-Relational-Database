# Agent service: LangChain + Pydantic with strict validation.
[project]
name = "agent-service"
version = "0.1.0"
requires-python = "==3.12.*"
dependencies = ["pydantic>=2.7", "langchain>=0.2", "httpx>=0.28.1"]

[tool.ruff]
line-length = 100

[tool.mypy]
strict = true
