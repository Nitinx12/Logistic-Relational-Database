# Dashboard: Streamlit chat + read-only charts.
[project]
name = "dashboard"
version = "0.1.0"
requires-python = "==3.12.*"
dependencies = ["streamlit>=1.35", "pandas>=2.2", "psycopg2-binary>=2.9", "pymongo>=4.6", "httpx>=0.27"]

[tool.ruff]
line-length = 100
